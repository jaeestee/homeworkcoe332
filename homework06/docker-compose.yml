---
version: "3"

services:
    flask-app:
        build:
            context: ./
            dockerfile: ./Dockerfile
        ports:
            - 5000:5000
        image: jaeestee/gene_api
        volumes:
            - ./config.yaml:/config.yaml